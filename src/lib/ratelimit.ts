const buckets=new Map<string,number[]>(); export function rateLimited(key:string,limit:number,windowMs:number){const now=Date.now();const a=(buckets.get(key)??[]).filter(t=>now-t<windowMs);a.push(now);buckets.set(key,a);return a.length>limit} export function clientIp(req:Request){return req.headers.get("x-forwarded-for")?.split(",")[0]?.trim()??"local"}
