import {z} from "zod"; export const requestSchema=z.object({name:z.string().trim().min(2,"Укажите имя").max(80),phone:z.string().trim().regex(/^[+\d][\d\s()\-]{8,19}$/,"Проверьте номер телефона"),make:z.string().trim().min(2,"Укажите марку"),year:z.coerce.number().int().min(1980).max(new Date().getFullYear()+1),model:z.string().trim().min(1,"Укажите модель"),engine:z.string().trim().min(1,"Укажите двигатель"),serviceType:z.string().min(2),comment:z.string().max(1200).optional(),preferredTime:z.string().optional(),agree:z.literal(true,{message:"Нужно согласие на обработку данных"}),website:z.string().max(100).optional()});
